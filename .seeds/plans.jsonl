{"id":"pl-ef44","seed":"os-eco-f066","template":"feature","status":"active","revision":1,"sections":{"context":"The L5 uplift mission has now ported the toolkit (Milestone 0) and uplifted the five sub-repos (canopy, plot, mulch, seeds, burrow) to Level 5. The final milestone seals the os-eco META repo itself: it has no package.json, no test suite, no lint pipeline, and ships no code — but the auditor still inspects governance hygiene (AGENTS.md, .gitignore, .github/ scaffolding, label taxonomy, toolkit discoverability) at the root. Currently the root has minimal .github/ (only auto-merge.yml + an EMPTY ISSUE_TEMPLATE directory), no AGENTS.md, no dependabot.yml, no labels.yml, no sync-labels workflow, and a thin .gitignore. This feature closes those gaps so the final /readiness-report can declare the meta-repo at Level 5, completing VAL-OS-ECO-001..007 + FINAL and feeding the cross-area VAL-CROSS-001..006 invariants.","approach":"Close the four governance/discoverability gaps for the os-eco META repo (no sub-repos touched) by porting the relevant artifacts out of templates/l5-toolkit/github/ into the os-eco root .github/ tree, hardening the root .gitignore, and pointing top-level docs at the L5 toolkit. Order is strict: governance files first, then gitignore + labels, then toolkit discoverability, then the milestone seal. The seal commit is the final wrap-up of the whole L5 uplift mission across the ecosystem.","steps":[{"title":"Add os-eco governance files (AGENTS.md, .github scaffolding)"},{"title":"Extend root .gitignore and add labels.yml + sync-labels workflow"},{"title":"Make L5 toolkit discoverable from root docs"},{"title":"Finalize for audit: verify governance, request /readiness-report"}],"acceptance":["AGENTS.md exists at /Users/jayminwest/Projects/os-eco/AGENTS.md (>2KB) with an Ecosystem section documenting the eight sub-repos and the cross-cutting .seeds/.mulch/.canopy/.plot/ directories.",".github/dependabot.yml exists and declares a weekly github-actions ecosystem with a cooldown block (no npm ecosystem block — root has no package.json).",".github/pull_request_template.md exists at os-eco root with Summary / Changes / Test plan sections.",".github/ISSUE_TEMPLATE/{bug_report.yml,feature_request.yml,config.yml} all exist and are non-empty.","Root .gitignore preserves the existing sub-repo + .run-plan-logs ignores AND adds .env, .env.* (with !.env.example), .idea/, .vscode/, .DS_Store, *.log.",".github/labels.yml exists with the canonical P0–P3 priority/area/kind/status taxonomy AND .github/workflows/sync-labels.yml exists and mirrors warren's pattern.","README.md or CLAUDE.md at the os-eco root contains a clearly-marked L5 Toolkit section linking to templates/l5-toolkit/README.md.","git status clean on os-eco main after all four children commit; the four feature commits each name the VAL-OS-ECO-* criteria they close in their messages.","The final child's salient handoff instructs the user to run /readiness-report from /Users/jayminwest/Projects/os-eco/ to produce .factory-mission-scratch/os-eco-readiness-report.md."]},"children":["os-eco-352d","os-eco-9c17","os-eco-d8f0","os-eco-3ce3"],"createdAt":"2026-05-28T03:18:33.503Z","updatedAt":"2026-05-28T16:58:54.729Z","name":"L5 uplift: os-eco root"}
{"id":"pl-760e","seed":"os-eco-3ee9","template":"feature","status":"done","revision":1,"sections":{"context":"The eight os-eco sub-repos run divergent quality gates. warren uses a 12-gate && chain with verbose names (check:file-sizes, check:debt-markers, check:duplicates, validate:agents-md) plus a check:ci-parity gate it originated; plot/mulch/seeds/trellis use the terse l5-toolkit names (check:size/debt/dups/deps/agents/coverage) in plain && chains; canopy omits check:coverage from its chain; burrow and sapling have no check:all at all (only test/lint/typecheck). No repo has a quiet runner, a `verify` alias, or (except warren) ci-parity. The sibling kota-monorepo solved this exact drift by freezing a canonical check:all standard: an ordered gate list with one canonical name per gate, a byte-identical scripts/check-all.ts quiet runner (one aligned line per gate, signatures-only on failure), `verify` aliased to check:all everywhere, and a check:ci-parity meta-gate (itself ported FROM warren) that fails any CI step not reachable from check:all. os-eco already owns the seed of this in templates/l5-toolkit (canonical terse names + check:* scripts) but stopped short of a frozen standard, the quiet runner, the verify alias, and fleet-wide coverage. This root tracker holds the cross-repo foundation (the standard doc + the portable runner/ci-parity artifacts in templates/l5-toolkit) and coordinates the eight per-repo conformance plans that consume it.","approach":"Land the cross-repo foundation here, then let each repo conform via its own sd plan (warren, burrow, plot, mulch, seeds, canopy, sapling, trellis). Step 1 writes docs/check-all-standard.md freezing the canonical terse vocabulary (matching templates/l5-toolkit precedent, with warren renaming its verbose keys), the ordered core gate set, a conditional-gate registry (check:bundle-size, gen:docs:check, gen:openapi:check), the quiet-output contract, the verify rule, and the ci-parity rule. Step 2 builds the portable quiet runner + generalized ci-parity into templates/l5-toolkit/scripts (with tests) and updates package-scripts.json + the toolkit README, porting warren's existing scripts/check-ci-parity.ts as the reference. Step 3 is the fleet close-out: once the eight per-repo plans land, verify byte-identical runners + canonical names + green ci-parity + quiet output across all repos, update root CLAUDE.md, and record the ecosystem decision. Cross-store sd dependencies can't be expressed, so the per-repo plans reference these foundational artifacts in prose; this tracker is the coordination point.","alternatives":[{"name":"One root plan that edits all eight repos directly","rejected_because":"Each sub-repo has its own .seeds/ store and git history; warren's monorepo model is one-branch-one-PR per repo, so conformance work belongs in each repo's own plan, not a single cross-repo plan that can't open eight PRs."},{"name":"Keep warren's verbose gate names as canonical and rename the other repos","rejected_because":"templates/l5-toolkit already documents the terse names (check:size/debt/dups/deps/agents/coverage) as canonical and four repos already use them; renaming one repo (warren) is far less churn than renaming five."}],"steps":[{"title":"Author docs/check-all-standard.md at the os-eco root: freeze the canonical terse gate vocabulary (check:size/debt/dups/deps/agents/coverage + lint/typecheck), the ordered core gate list (lint, typecheck, check:agents, check:dups, check:deps, check:size, check:debt, [conditional gates], check:coverage, check:ci-parity LAST), a conditional-gate registry (check:bundle-size for bundle producers, gen:docs:check for doc generators, gen:openapi:check for OpenAPI servers), the quiet-output contract (one aligned `<status> <gate> (N.Ns)` line per gate; on failure only failing gate names + parsed test signatures; CHECK_ALL_VERBOSE/--bail escape hatches), the `verify` = `bun run check:all` rule, and the check:ci-parity rule. Include a per-repo reconciliation table mapping each repo's current names to canonical (esp. warren's verbose keys). Doc-only; changes no repo behavior.","type":"task","priority":1,"blocks":[2]},{"title":"Build the portable quiet runner + generalized ci-parity in templates/l5-toolkit/scripts: add check-all.ts (exports a GATES array run as data, captures each gate's output, prints the aligned per-gate lines + tally on success and signatures-only on failure, honors CHECK_ALL_VERBOSE and --bail) and check-ci-parity.ts (imports GATES from check-all.ts, parses .github/workflows/ci*.yml, fails on any `bun run <x>` not reachable from check:all, with ALIASES/CI_ONLY escape hatches) plus co-located *.test.ts; port warren/scripts/check-ci-parity.ts as the reference implementation. Update templates/l5-toolkit/package-scripts.json so check:all becomes `bun scripts/check-all.ts`, add `verify`: `bun run check:all` and `check:ci-parity`: `bun scripts/check-ci-parity.ts`, add the `yaml` devDep. Document the runner + verify + ci-parity wiring in templates/l5-toolkit/README.md.","type":"task","priority":1,"blocks":[3]},{"title":"Fleet close-out and parity verification: after the eight per-repo conformance plans land, run `bun run check:all` and `bun run verify` in every sub-repo and confirm each uses the byte-identical scripts/check-all.ts runner, the canonical gate names in the standard's order, a green check:ci-parity, and the quiet-output contract (one line per gate, signatures-only on failure). Update the root CLAUDE.md 'Build & Test Commands' section to standardize on `bun run check:all` / `bun run verify`, and record an ecosystem-domain mulch decision capturing the adopted standard.","type":"task","priority":2,"blocks":[]}],"risks":["Cross-store sd plans can't hard-link dependencies, so the per-repo plans depend on this tracker's steps 1-2 only by prose convention; landing a repo plan before the standard/runner exists would create rework.","The standard's name reconciliation (warren verbose -> terse) touches CLAUDE.md and CI references; missing a reference leaves a dangling script name."],"acceptance":["docs/check-all-standard.md exists at the os-eco root with the canonical ordered gate list, conditional-gate registry, quiet-output contract, verify rule, ci-parity rule, and a per-repo reconciliation table.","templates/l5-toolkit/scripts/check-all.ts and check-ci-parity.ts (with tests) exist and templates/l5-toolkit/package-scripts.json wires check:all -> the runner, plus verify and check:ci-parity.","All eight sub-repos run `bun run check:all` and `bun run verify` green with the byte-identical runner, canonical names, and a passing check:ci-parity.","Root CLAUDE.md documents the standard and an ecosystem mulch decision records it."]},"children":["os-eco-9048","os-eco-5db7","os-eco-9a3a"],"createdAt":"2026-06-12T04:50:50.648Z","updatedAt":"2026-06-12T06:03:17.121Z","name":"Adopt canonical check:all standard across the os-eco fleet","outcome":"success"}
{"id":"pl-b7eb","seed":"os-eco-35ed","template":"feature","status":"approved","revision":1,"sections":{"context":"Forkar um repo da fleet (warren, burrow, etc.) pra rodar a esteira propria exige varios pre-requisitos que um fork fresco NAO tem, e quando faltam, os PRs do plan-run travam silenciosamente (o warren so faz polling do merge e da timeout). Isso ja mordeu de verdade: o plan-run pl-a77b no fork do warren abriu 2 PRs que nunca mergearam porque a CI NUNCA rodou (GitHub desabilita Actions em fork por padrao; precisa de um clique unico na UI que a API nao destrava de forma confiavel). Os outros pre-requisitos: secret AUTO_MERGE_PAT, allow_auto_merge habilitado no repo, branch protection com o check ci obrigatorio (e enforce_admins=false pro dono ainda dar push direto), e o PR ser autorado pelo dono do repo (condicao do auto-merge.yml). Falta uma ferramenta que automatize o automatizavel e AUDITE a prontidao, deixando explicito o passo manual.","approach":"Tres artefatos no root do os-eco (cross-tool, vale pra fleet inteira). (1) scripts/fork-onboarding.sh: idempotente, faz o automatizavel via gh/git (re-aponta remotes origin=fork/upstream=jayminwest; sincroniza o fork com upstream; seta AUTO_MERGE_PAT; habilita allow_auto_merge; configura branch protection com required check ci e enforce_admins=false) e imprime CLARAMENTE o passo manual UI-only (habilitar Actions na aba Actions do fork), porque nao da pra automatizar de forma confiavel. (2) scripts/fork-doctor.sh: AUDITA a prontidao de um fork e reporta PASS/gaps com hint de fix, cobrindo justamente o que travou: Actions runs > 0 (senao: workflows nao habilitados, clicar no banner da aba Actions), AUTO_MERGE_PAT presente, allow_auto_merge=true, branch protection exige ci, e PR-author == dono do repo. (3) docs/fork-onboarding.md: runbook com a ordem dos passos, o passo UI-only em destaque, e os gotchas; rodar o doctor nos forks existentes da fleet e registrar o status. Scripts sao bash + gh + git (rodam na maquina do usuario, nao em sandbox); a esteira produz os arquivos, o usuario executa.","alternatives":[{"name":"So documentar (runbook), sem script/doctor","rejected_because":"o erro e silencioso e recorrente por fork; um doctor que audita pega o gap antes do plan-run travar, doc sozinha nao"},{"name":"Tentar automatizar o enable de Actions do fork via API","rejected_because":"o gate de workflows-em-fork e UI-only (a API actions/permissions diz enabled=true e os workflows aparecem active, mas nada roda ate o clique no banner); o script deve DETECTAR e instruir, nao fingir que automatiza"},{"name":"Resolver caso a caso na mao a cada fork","rejected_because":"a fleet tem 8 forks; sem script+doctor isso se repete e erra; centralizar no root evita drift"}],"steps":[{"title":"scripts/fork-onboarding.sh: idempotente, faz o automatizavel (remotes, sync upstream, AUTO_MERGE_PAT, allow_auto_merge, branch protection com required check ci + enforce_admins=false) e imprime em destaque o passo manual UI-only (habilitar Actions no fork)","type":"feature","priority":1,"labels":["scripts","ci"],"blocks":[3]},{"title":"scripts/fork-doctor.sh: audita prontidao do fork (Actions runs>0, AUTO_MERGE_PAT, allow_auto_merge, branch protection exige ci, PR-author==owner) e reporta PASS/gaps com hint de fix","type":"feature","priority":1,"labels":["scripts","ci"],"blocks":[3]},{"title":"docs/fork-onboarding.md (runbook com o passo UI-only em destaque + gotchas) + rodar o doctor nos forks da fleet e registrar o status; testes/shellcheck dos scripts","type":"task","priority":2,"labels":["docs","validation"],"blocks":[]}],"risks":["O passo de habilitar Actions e UI-only: o script NAO pode fingir que automatiza; deve detectar (runs==0) e instruir o clique, senao da falsa sensacao de pronto","Branch protection mal configurada pode bloquear o dono: usar enforce_admins=false pra o dono ainda dar push direto, e exigir so o check ci","Idempotencia: rodar o onboarding 2x nao pode quebrar (checar estado antes de setar; nao duplicar remotes/secret)","AUTO_MERGE_PAT e segredo: o script recebe via env/stdin, nunca hardcoded nem ecoado em log","Cache de gh/permissoes: o doctor deve ler o estado real (gh api) e nao confiar so em campos que mentem (workflows active mas runs==0)"],"acceptance":["scripts/fork-onboarding.sh roda idempotente e configura remotes, sync, AUTO_MERGE_PAT, allow_auto_merge e branch protection (required check ci, enforce_admins=false), imprimindo o passo manual de habilitar Actions em destaque","scripts/fork-doctor.sh reporta PASS quando o fork esta pronto e aponta cada gap com hint (em especial: Actions runs==0 -> habilitar workflows na UI)","docs/fork-onboarding.md descreve a ordem dos passos com o passo UI-only em destaque e os gotchas","o doctor foi rodado nos forks existentes da fleet e o status registrado","scripts passam shellcheck/teste; gate verde em todos os PRs do plano"]},"children":["os-eco-3d68","os-eco-ac01","os-eco-78c2"],"createdAt":"2026-06-20T11:01:24.750Z","updatedAt":"2026-06-20T11:01:24.750Z","name":"os-eco: fork onboarding (script + doctor + runbook) pra preparar forks da fleet pra esteira"}
